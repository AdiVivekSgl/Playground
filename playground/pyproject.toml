[project]
name = "playground"
version = "0.0.1"
description = "Custom API scripts for ERPNext"
requires-python = ">=3.10"

[build-system]
requires = ["flit_core >=3.2.0,<4"]
build-backend = "flit_core.buildapi"
